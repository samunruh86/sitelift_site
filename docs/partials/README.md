# Partials

Edit `header.html` and `footer.html` to update the shared navigation and footer.

To rebuild the static pages with injected partials:

```sh
npm run build
```

Built files are written to `docs/dist`.
